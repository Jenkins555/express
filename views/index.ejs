<!DOCTYPE html>
<html>
<head>
    <title>Список книг</title>
</head>
<body>
    <h1>Список книг</h1>
    <ul>
        <% books.forEach((book) => { %>
            <li><a href="/books/<%= book.id %>"><%= book.title %></a></li>
        <% }); %>
    </ul>
</body>
</html>